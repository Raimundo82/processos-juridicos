@model Processos_Juridicos.DTOs.CrimeTypeDto;
@using Processos_Juridicos.DTOs;

@{
    ViewData["Title"] = "Editar Tipo de Crime";
}

<h1>@ViewData["Title"]</h1>

<div class="container-lg my-3">
    <div class="card mb-3">
        <h2>@ViewData["Title"]</h2>
    </div>

    <div class="card">
        <form asp-action="Edit" method="post" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <input type="hidden" asp-for="CrimeTypeId" />

            <partial name="Shared/_Form" />

            <div class="d-flex justify-content-center mb-4">
                <div class="form-group mt-3 text-center">
                    <input type="submit" value="Guardar" class="btn btn-primary me-2" />
                    <a asp-action="List" class="btn btn-secondary">Voltar à Lista</a>
                </div>
            </div>

        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
