{
    "name": "jstests",
    "version": "1.0.0",
    "type": "module",
    "scripts": {
        "test": "vitest run --coverage"
    },
    "devDependencies": {
        "vitest": "^3.2.4",
        "jsdom": "^24.0.0",
        "@vitest/coverage-v8": "^3.2.4"
    }
}
